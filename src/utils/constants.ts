export const SCHEMA = "starglobal";

export enum ModelName {
  WishList = "wishList",
  WishListItem = "wishListItem",
}

export enum TableName {
  WishList = "WishList",
  WishListItem = "WishListItem",
}

export const USER_ID_REQUEST_HEADER = "x-user-id";
